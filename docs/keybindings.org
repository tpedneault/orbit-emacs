#+title: keybindings

* Global Shortcuts

| Key       | Action                     | Notes                  |
|-----------+----------------------------+------------------------|
| =SPC SPC= | =mod-keys-find-file-dwim=  | project file finder in project, =find-file= otherwise |
| =SPC .=   | =consult-buffer=           | fast buffer switch     |
| =SPC /=   | =mod-project-search=       | ripgrep in project     |
| =SPC :=   | =execute-extended-command= | =M-x=                  |
| =SPC ,=   | local leader               | same target as =SPC m= |
| =SPC v=   | =er/expand-region=         | grow selection         |

* =SPC f= Files

| Key         | Action                      | Notes             |
|-------------+-----------------------------+-------------------|
| =SPC f c=   | config subgroup             | config access     |
| =SPC f c i= | open =init.el=              | main entry point  |
| =SPC f c e= | open =early-init.el=        | early startup     |
| =SPC f c m= | open =modules/=             | module directory  |
| =SPC f c a= | open =AGENTS.md=            | agent instructions |
| =SPC f c d= | open =docs/=                | docs directory    |
| =SPC f c r= | reload config               | reload =init.el=  |
| =SPC f c u= | open user =config.el=       | user overrides    |
| =SPC f c U= | open user dir               | user-local layer  |
| =SPC f c S= | open user snippets          | user snippets dir |
| =SPC f c s= | snippet maintenance group   | snippet upkeep    |
| =SPC f c s e= | =yas-visit-snippet-file=  | edit snippet file |
| =SPC f c s n= | =yas-new-snippet=         | new snippet       |
| =SPC f c s r= | =yas-reload-all=          | reload snippets   |
| =SPC f f=   | =mod-context-open-path=     | open file/path in matching context |
| =SPC f r=   | =mod-core-recentf-open=     | recent files, current project first when available |
| =SPC f s=   | =save-buffer=               | save current file |
| =SPC f y a= | copy absolute file path     | current buffer file |
| =SPC f y r= | copy project-relative path  | falls back to absolute outside a project |
| =SPC f y d= | copy current directory path | file dir or =default-directory= |

* =SPC b= Buffers

| Key       | Action                | Notes                         |
|-----------+-----------------------+-------------------------------|
| =SPC b b= | =consult-buffer=      | =switch-to-buffer= is remapped |
| =SPC b d= | =kill-current-buffer= | kill buffer                   |
| =SPC b r= | =revert-buffer=       | revert from disk              |

* =SPC c= Code

| Key       | Action                           | Notes                   |
|-----------+----------------------------------+-------------------------|
| =SPC c d= | =mod-core-duplicate-line-or-region= | duplicate region or line |

* =SPC p= Projects

| Key       | Action                   | Notes                |
|-----------+--------------------------+----------------------|
| =SPC p a= | =mod-project-add=        | remember project     |
| =SPC p d= | =mod-project-forget=     | forget known project |
| =SPC p p= | =mod-project-switch=    | switch to =edit/<project>= |
| =SPC p f= | =project-find-file=      | find project file    |
| =SPC p R= | =mod-project-replace=    | project query replace |
| =SPC p s= | =mod-project-search=     | project ripgrep      |

* =SPC w= Windows

| Key       | Action                  | Notes          |
|-----------+-------------------------+----------------|
| =SPC w w= | =other-window=          | cycle windows  |
| =SPC w h= | =windmove-left=         | move left      |
| =SPC w j= | =windmove-down=         | move down      |
| =SPC w k= | =windmove-up=           | move up        |
| =SPC w l= | =windmove-right=        | move right     |
| =SPC w d= | =delete-window=         | close window   |
| =SPC w o= | =delete-other-windows=  | single window  |
| =SPC w s= | =split-window-below=    | split below    |
| =SPC w v= | =split-window-right=    | split right    |
| =SPC w u= | =winner-undo=           | undo layout    |
| =SPC w U= | =winner-redo=           | redo layout    |

* =SPC g= Git

| Key       | Action            | Notes          |
|-----------+-------------------+----------------|
| =SPC g f f= | =mod-git-find-file-from-revision= | open file from revision |
| =SPC g g= | =mod-git-status=  | Magit status   |
| =SPC g l= | =mod-git-log=     | repo log       |
| =SPC g b= | =mod-git-blame=   | file blame     |

* =SPC o= Utility Bay

| Key       | Action                    | Notes            |
|-----------+---------------------------+------------------|
| =SPC o o= | =mod-utility-toggle=      | close or reopen  |
| =SPC o O= | =mod-core-open-at-point=  | open URL or file path at point |
| =SPC o s= | =mod-utility-shell=       | built-in shell   |
| =SPC o m= | =mod-utility-messages=    | =*Messages*=     |
| =SPC o h= | =mod-utility-help=        | =*Help*=         |
| =SPC o c= | =mod-utility-compilation= | =*compilation*=  |

* =SPC n= Notes / Org

| Key       | Action                     | Notes              |
|-----------+----------------------------+--------------------|
| =SPC n n= | =mod-org-open-notes=       | open =notes.org=   |
| =SPC n a= | =mod-org-open-agenda=      | work dashboard     |
| =SPC n c g= | =org-clock-goto=         | jump to active clock |
| =SPC n c o= | =org-clock-out=          | clock out          |
| =SPC n c c= | =org-clock-cancel=       | cancel active clock |
| =SPC n C= | =mod-org-capture=          | capture menu       |
| =SPC n t= | =mod-org-capture-inbox-task= | inbox task       |
| =SPC n j j= | =mod-org-capture-journal= | journal entry    |
| =SPC n j J= | =mod-jira-open-file=     | open =jira.org=   |
| =SPC n j c= | =mod-jira-add-comment=   | add Jira comment   |
| =SPC n j i= | =mod-jira-import-issue=  | import issue by key or number |
| =SPC n j s= | =mod-jira-sync=          | sync Jira issues into agenda-visible =jira.org= |
| =SPC n j r= | =mod-jira-refresh-issue= | refresh issue at point |
| =SPC n j t= | =mod-jira-transition-issue= | transition issue |
| =SPC n j o= | =mod-jira-open-issue=    | open issue at point |
| =SPC n j w= | =mod-jira-log-work=      | submit worklog from Org clock |
| =SPC n j W= | =mod-jira-log-work-manual= | submit manual Jira worklog |
| =SPC n N= | =mod-org-capture-note=     | quick note         |

* =SPC x= Contexts

| Key       | Action                | Notes                                  |
|-----------+-----------------------+----------------------------------------|
| =SPC x x= | =mod-context-switch=  | switch or create by name               |
| =SPC x n= | =mod-context-new=     | new blank context                      |
| =SPC x d= | =mod-context-delete=  | delete context                         |
| =SPC x r= | =mod-context-rename=  | rename context                         |
| =SPC x [= | =mod-context-previous= | previous context                      |
| =SPC x ]= | =mod-context-next=    | next context                           |
| =SPC x e= | =mod-context-editor=  | prompt project if needed; file picker for empty =edit= |
| =SPC x g= | =mod-context-git=     | prompt project if needed; init once, later switch only |
| =SPC x f= | =mod-context-files=   | Dired stays here, project files open in =edit/<project>=, loose files in =edit/loose= |
| =SPC x o= | =mod-context-notes=   | =notes= context                        |
| =SPC x a= | =mod-context-agenda=  | =agenda= dashboard context             |
| =SPC x s= | =mod-context-scratch= | =scratch= context                      |

* =SPC h= Help

| Key       | Action                  | Notes              |
|-----------+-------------------------+--------------------|
| =SPC h b= | =xref-go-back=          | docs/code jump back |
| =SPC h B= | =xref-go-forward=       | docs/code jump forward |
| =SPC h k= | =describe-key=          | describe key       |
| =SPC h f= | =describe-function=     | describe function  |
| =SPC h d= | open docs manual        | opens =docs/manual.org= read-only |
| =SPC h D= | open docs directory     | =docs/=            |
| =SPC h m= | =describe-mode=         | describe mode      |
| =SPC h v= | =describe-variable=     | describe variable  |

* =SPC i= Insert

| Key       | Action               | Notes             |
|-----------+----------------------+-------------------|
| =SPC i s= | =yas-insert-snippet= | insert snippet    |

* =SPC t= Toggles

| Key       | Action                | Notes                    |
|-----------+-----------------------+--------------------------|
| =SPC t c= | =mod-ui-toggle-fill-column-indicator= | toggle fill-column indicator |
| =SPC t w= | =mod-ui-toggle-wrap=  | toggle wrap / truncation |

* Editing

| Key   | Action                              | Notes                  |
|-------+-------------------------------------+------------------------|
| =M-j= | =mod-core-move-line-or-region-down= | move line or region down |
| =M-k= | =mod-core-move-line-or-region-up=   | move line or region up   |

* =SPC q= Quit / Session

| Key       | Action                  | Notes                |
|-----------+-------------------------+----------------------|
| =SPC q s= | =mod-session-save=      | save session         |
| =SPC q l= | =mod-session-load=      | load session         |
| =SPC q q= | =save-buffers-kill-terminal= | quit Emacs     |

* =SPC m= Local Leader

** Org Buffer

| Key         | Action                      | Notes               |
|-------------+-----------------------------+---------------------|
| =RET=       | follow Org link             | normal state        |
| =SPC m o=   | =consult-outline=           | outline jump        |
| =SPC m n=   | =org-next-visible-heading=  | next heading        |
| =SPC m p=   | =org-previous-visible-heading= | previous heading |
| =SPC m u=   | =outline-up-heading=        | up heading          |
| =SPC m TAB= | =org-cycle=                 | cycle subtree       |
| =SPC m t=   | =org-todo=                  | TODO state          |
| =SPC m s=   | =org-schedule=              | schedule            |
| =SPC m d=   | =org-deadline=              | deadline            |
| =SPC m r=   | =org-refile=                | refile heading      |
| =SPC m a=   | =org-archive-subtree=       | archive subtree     |
| =SPC m P=   | =org-priority=              | priority            |
| =SPC m J=   | =org-metadown=              | move subtree down   |
| =SPC m K=   | =org-metaup=                | move subtree up     |
| =SPC m H=   | =org-metaleft=              | promote heading     |
| =SPC m L=   | =org-metaright=             | demote heading      |
| =SPC m i h= | =org-insert-heading=        | insert heading      |
| =SPC m i t= | =org-insert-todo-heading=   | insert TODO heading |
| =SPC m i s= | =org-insert-subheading=     | insert subheading   |
| =SPC m c x= | =org-toggle-checkbox=       | toggle checkbox     |
| =SPC m c i= | =org-clock-in=              | clock in            |
| =SPC m c o= | =org-clock-out=             | clock out           |
| =SPC m c c= | =org-clock-cancel=          | cancel clock        |
| =SPC m c g= | =org-clock-goto=            | jump to active clock |
| =SPC m z=   | =org-narrow-to-subtree=     | narrow subtree      |
| =SPC m Z=   | =widen=                     | widen               |

** Agenda Buffer

| Key       | Action               | Notes                       |
|-----------+----------------------+-----------------------------|
| =j=       | =org-agenda-next-line= | Vim-style movement        |
| =k=       | =org-agenda-previous-line= | Vim-style movement   |
| =RET=     | =mod-org-agenda-visit= | open item in notes context |
| =SPC m c i= | =org-agenda-clock-in= | clock in selected item    |
| =SPC m c o= | =org-agenda-clock-out= | clock out                |
| =SPC m c c= | =org-agenda-clock-cancel= | cancel active clock   |
| =SPC m c g= | =org-agenda-clock-goto= | jump to active clock  |
| =SPC m t= | =org-agenda-todo=    | change state                |
| =SPC m s= | =org-agenda-schedule= | schedule                   |
| =SPC m d= | =org-agenda-deadline= | deadline                   |
| =SPC m r= | =org-agenda-refile=  | refile                      |
| =SPC m a= | =org-agenda-archive= | archive                     |
| =SPC m v= | =mod-org-agenda-visit= | visit selected item      |

** Tcl Buffer

| Key         | Action                   | Notes               |
|-------------+--------------------------+---------------------|
| =SPC m l=   | =mod-tcl-lint-file=      | lint current file   |
| =SPC m f=   | =mod-tcl-format-file=    | format current file |
| =SPC m F=   | =mod-tcl-fold-definitions= | fold definitions  |
| =SPC m TAB= | =mod-tcl-toggle-fold=    | toggle fold at point |
| =SPC m g=   | =mod-tcl-find-tag=       | jump to definition  |
| =SPC m s=   | =mod-tcl-search-symbols= | search Tcl symbols  |
| =SPC m v=   | =mod-tcl-validate-tooling= | validate tools    |
| =SPC m r=   | =mod-tcl-rebuild-tags=   | rebuild project TAGS |
| =SPC m e=   | =mod-tcl-show-output=    | Tcl tool output     |
| =SPC m d d= | =mod-tcl-docs-manual=    | project docs manual |
| =SPC m d s= | =mod-tcl-docs-search=    | search docs         |
| =SPC m d p= | =mod-tcl-docs-at-point=  | docs at point       |

** Python Buffer

| Key         | Action                    | Notes                    |
|-------------+---------------------------+--------------------------|
| =SPC m e s= | =mod-python-eglot=        | start Eglot              |
| =SPC m e R= | =mod-python-eglot-reconnect= | reconnect Eglot      |
| =SPC m e r= | =eglot-rename=            | rename symbol            |
| =SPC m e h= | =mod-python-show-docs=    | show docs / hover        |
| =SPC m f b= | =mod-python-format-buffer= | format buffer           |
| =SPC m g d= | =xref-find-definitions=   | go to definition         |
| =SPC m g r= | =xref-find-references=    | find references          |
| =SPC m r f= | =mod-python-run-file=     | run current file         |
| =SPC m r m= | =mod-python-run-module=   | run module               |
| =SPC m d d= | =mod-python-debug-file=   | debug current file       |
| =SPC m d b= | =dape-breakpoint-toggle=  | toggle breakpoint       |
| =SPC m d x= | =dape-breakpoint-remove-at-point= | remove breakpoint |
| =SPC m d X= | =dape-breakpoint-remove-all= | remove all breakpoints |
| =SPC m d e= | =dape-breakpoint-expression= | expression breakpoint |
| =SPC m d l= | =dape-breakpoint-log=     | log breakpoint          |
| =SPC m d f= | =dape-breakpoint-function= | function breakpoint    |
| =SPC m d w= | =dape-watch-dwim=         | watch expression        |
| =SPC m d c= | =dape-continue=           | continue                |
| =SPC m d p= | =dape-pause=              | pause                   |
| =SPC m d n= | =dape-next=               | step over               |
| =SPC m d i= | =dape-step-in=            | step in                 |
| =SPC m d o= | =dape-step-out=           | step out                |
| =SPC m d r= | =mod-python-debug-restart= | restart or relaunch    |
| =SPC m d q= | =dape-kill=               | quit debug session      |
| =SPC m d R= | =dape-repl=               | debugger REPL           |
| =SPC m d I= | =dape-info=               | debugger info buffers   |

Active Python debug session single-key controls:

| Key | Action | Notes |
|-----+--------+-------|
| =b= | =dape-breakpoint-toggle= | source / Dape buffers |
| =x= | =dape-breakpoint-remove-at-point= | source buffers |
| =c= | =dape-continue= | continue |
| =n= | =dape-next= | step over |
| =i= | =dape-step-in= | step in |
| =o= | =dape-step-out= | step out |
| =p= | =dape-pause= | pause |
| =r= | =mod-python-debug-restart= | restart or relaunch |
| =q= | =dape-kill= | quit session |
| =w= | =dape-watch-dwim= | watch expression |
| =R= | =dape-repl= | debugger REPL |
| =I= | =dape-info= | debugger info |
